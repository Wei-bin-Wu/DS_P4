<%= form_with(model: examene) do |form| %>
  <% if examene.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(examene.errors.count, "error") %> prohibited this examene from being saved:</h2>

      <ul>
        <% examene.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha %>
  </div>

  <div class="field">
    <%= form.label :asignaturas_id %>
    <%= form.text_field :asignaturas_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
