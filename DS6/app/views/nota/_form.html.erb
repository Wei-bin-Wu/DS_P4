<%= form_with(model: notum) do |form| %>
  <% if notum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notum.errors.count, "error") %> prohibited this notum from being saved:</h2>

      <ul>
        <% notum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label singularize(:alumnos_id) %>
    <%= form.text_field :alumnos_id %>
  </div>

  <div class="field">
    <%= form.label singularize(:examenes_id) %>
    <%= form.text_field :examenes_id %>
  </div>

  <div class="field">
    <%= form.label singularize(:nota) %>
    <%= form.number_field :nota %>
  </div>

  <div class="field">
    <%= form.label :foto %>
    <%= form.text_field :foto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
